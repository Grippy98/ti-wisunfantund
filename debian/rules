#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	./bootstrap.sh
	dh_auto_configure -- --sysconfdir=/etc --prefix=/usr

override_dh_auto_install:
	dh_auto_install
	# Clean up any unnecessary files if needed
